var asyncFunction = require('./lib/asyncFunction');
var asyncValue = require('./lib/asyncValue');
var defaultValue = require('./lib/defaultValue');

module.exports = {
  asyncFunction: asyncFunction,
  asyncValue: asyncValue,
  defaultValue: defaultValue,
};
